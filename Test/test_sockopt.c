#include "flip.h"
#include <stdio.h>
#include <stdlib.h>


meta_header metaHdr;
flip_header flipHdr;

int main(int argc, char *argv[])
{	
	int k = setsockopt (int optname, uint32_t optval, int optlen);
	if (k < 0){ 
		printf("Something went wrong oh-oh\n");
	}
	
}